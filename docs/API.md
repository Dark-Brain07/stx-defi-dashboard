# API Reference

## GET /api/tvl
Returns total TVL and protocol breakdown.

## GET /api/wallet/:address
Returns wallet balances and recent transactions.

## GET /api/network
Returns block height, mempool stats, and STX price.

## POST /api/chainhooks/webhook
Ingests Hiro Chainhook events for real-time tracking.