import{describe,it,expect}from"vitest";import{OfferManager}from"../../lib/nft/marketplace/OfferManager";
describe("OfferManager",()=>{it("ok",()=>expect(new OfferManager()).toBeDefined());it("set/get",()=>{const x=new OfferManager();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new OfferManager();x.disable();expect(x.isActive()).toBe(false);});});