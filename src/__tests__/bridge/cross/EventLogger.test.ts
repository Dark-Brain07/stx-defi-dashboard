import{describe,it,expect}from"vitest";import{EventLogger}from"../../lib/bridge/cross/EventLogger";
describe("EventLogger",()=>{it("ok",()=>expect(new EventLogger()).toBeDefined());it("set/get",()=>{const x=new EventLogger();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new EventLogger();x.disable();expect(x.isActive()).toBe(false);});});