import{describe,it,expect}from"vitest";import{LimitOrder}from"../../lib/defi/dex/LimitOrder";
describe("LimitOrder",()=>{it("ok",()=>expect(new LimitOrder()).toBeDefined());it("state",()=>{const x=new LimitOrder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LimitOrder();x.disable();expect(x.isActive()).toBe(false);});});