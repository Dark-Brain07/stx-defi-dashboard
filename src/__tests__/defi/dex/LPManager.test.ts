import{describe,it,expect}from"vitest";import{LPManager}from"../../lib/defi/dex/LPManager";
describe("LPManager",()=>{it("ok",()=>expect(new LPManager()).toBeDefined());it("state",()=>{const x=new LPManager();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LPManager();x.disable();expect(x.isActive()).toBe(false);});});