import{describe,it,expect}from"vitest";import{SlashProtection}from"../../lib/defi/staking/SlashProtection";
describe("SlashProtection",()=>{it("ok",()=>expect(new SlashProtection()).toBeDefined());it("state",()=>{const x=new SlashProtection();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SlashProtection();x.disable();expect(x.isActive()).toBe(false);});});