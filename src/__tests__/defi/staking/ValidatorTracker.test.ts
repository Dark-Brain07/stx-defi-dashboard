import{describe,it,expect}from"vitest";import{ValidatorTracker}from"../../lib/defi/staking/ValidatorTracker";
describe("ValidatorTracker",()=>{it("ok",()=>expect(new ValidatorTracker()).toBeDefined());it("state",()=>{const x=new ValidatorTracker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ValidatorTracker();x.disable();expect(x.isActive()).toBe(false);});});