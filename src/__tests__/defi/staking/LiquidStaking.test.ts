import{describe,it,expect}from"vitest";import{LiquidStaking}from"../../lib/defi/staking/LiquidStaking";
describe("LiquidStaking",()=>{it("ok",()=>expect(new LiquidStaking()).toBeDefined());it("state",()=>{const x=new LiquidStaking();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LiquidStaking();x.disable();expect(x.isActive()).toBe(false);});});