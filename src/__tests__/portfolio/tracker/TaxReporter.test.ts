import{describe,it,expect}from"vitest";import{TaxReporter}from"../../lib/portfolio/tracker/TaxReporter";
describe("TaxReporter",()=>{it("ok",()=>expect(new TaxReporter()).toBeDefined());it("state",()=>{const x=new TaxReporter();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TaxReporter();x.disable();expect(x.isActive()).toBe(false);});});