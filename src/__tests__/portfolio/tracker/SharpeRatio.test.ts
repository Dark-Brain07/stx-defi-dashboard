import{describe,it,expect}from"vitest";import{SharpeRatio}from"../../lib/portfolio/tracker/SharpeRatio";
describe("SharpeRatio",()=>{it("ok",()=>expect(new SharpeRatio()).toBeDefined());it("state",()=>{const x=new SharpeRatio();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SharpeRatio();x.disable();expect(x.isActive()).toBe(false);});});