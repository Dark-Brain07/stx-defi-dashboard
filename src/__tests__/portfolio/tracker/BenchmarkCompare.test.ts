import{describe,it,expect}from"vitest";import{BenchmarkCompare}from"../../lib/portfolio/tracker/BenchmarkCompare";
describe("BenchmarkCompare",()=>{it("ok",()=>expect(new BenchmarkCompare()).toBeDefined());it("state",()=>{const x=new BenchmarkCompare();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BenchmarkCompare();x.disable();expect(x.isActive()).toBe(false);});});